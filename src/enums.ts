export enum BodyShape {
  RECTANGLE = 'R',
  CIRCLE = 'C',
}

export enum ComponentType {
  SOLID_BODY = 'BODY',
  SENSOR = 'SENSOR',
  RENDER = 'RENDER',
  MOTOR = 'MOTOR',
  SOURCE = 'SOURCE',
  TRANSFORMABLE = 'TRANSFORMABLE',
}

export enum SubstanceType {
  LIGHT = 'LIGHT',
}
